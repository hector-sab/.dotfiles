-- NOTE: If you want to return to the previous
-- window (aka netrw), use <C-w> + p. That will
-- get the cursor to the previous window.
-- An alternative is to cycle through the windows
-- with <C-w> + w. Check :help window-move-cursor
vim.g.netrw_liststyle = 3
